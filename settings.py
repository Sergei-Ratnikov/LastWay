# settings.py
# =============================================================================
# ГЛОБАЛЬНЫЕ НАСТРОЙКИ ИГРЫ
# Содержит все константы: размеры окна, цвета, пути к папкам, задержки.
# =============================================================================

import os

# -----------------------------------------------------------------------------
# РАЗМЕРЫ ОКНА И КЛЕТКИ
# -----------------------------------------------------------------------------
WIDTH = 1024
HEIGHT = 768
TILE_SIZE = 48                     # Размер одного тайла в пикселях (квадрат)

# -----------------------------------------------------------------------------
# ЗАДЕРЖКА ДВИЖЕНИЯ ИГРОКА
# -----------------------------------------------------------------------------
MOVE_DELAY_MS = 150                # Миллисекунды между шагами (≈6-7 шагов/сек)

# -----------------------------------------------------------------------------
# ЦВЕТА (RGB)
# -----------------------------------------------------------------------------
BLACK = (0, 0, 0)
WHITE = (255, 255, 255)
GREEN = (0, 255, 0)
RED = (200, 0, 0)
BROWN = (139, 69, 19)
LIGHT_GREY = (150, 150, 150)
DARK_GREY = (40, 40, 40)
YELLOW = (255, 215, 0)
BLUE = (0, 0, 255)
NPC_COLOR = (0, 150, 200)          # Цвет прямоугольника NPC (сине-голубой)

# -----------------------------------------------------------------------------
# ПУТИ К ПАПКАМ С ДАННЫМИ
# -----------------------------------------------------------------------------
BASE_DIR = os.path.dirname(os.path.abspath(__file__))   # Корень проекта
DATA_DIR = os.path.join(BASE_DIR, "data")               # Папка data
MAPS_DIR = os.path.join(DATA_DIR, "maps")               # Карты
NPCS_DIR = os.path.join(DATA_DIR, "npcs")               # Данные NPC
DIALOGS_DIR = os.path.join(DATA_DIR, "dialogs")         # Диалоги